const char a, b[2], c = 123, d = 0xFF;
int b;
short z[2] = 's';
long adios;
int varaible, varaible;

// esto es un comentario magico

int main() {
	int funvar = funcion(a,b,c,1+,funcion(a,v),'c');
	int a[1][2] = {1,2,4};
	z = {1,2,3};
	return 1;
}

const char lol(int a, char b) {
	int a;
	char b = 1 + 2 +;

	a = b = b + 1 == !Z[0];

	if (a,b) {
		read(a);
		return;
	}
	else{
		return;
	}

	if(1==1)
		write(a);

	if(1==1) {
		return 0;
	}
	else {
		return 1;
	}

	/*
		Comentario para ignorar ^&^*&*&
	*/

	do {
		switch (indentifidcador) {
			case 1:
				var += 1;
			case 4:
				write("asdf");
			default:
				write(1+1);
				continue;
		}
	} while(a == 2);

	for (a = 1; a < 5; a++) {
		while(a)
			write (adios + vamos[1 - ye90]);

		variable1 += variable2 - !false;
		
	}
}